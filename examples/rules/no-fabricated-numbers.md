---
# 这条只能靠 T1 + T2 —— 没有任何工具开关能阻止「编一个数字」。
# 所以它是「T1 有存在意义」的那一类：T2 保证 confidence 必填，
# T1 那句话解释为什么。
appliesTo: ['*']
requiredFields: [confidence]
---

不确定的数字不要编。拿不到准确值时留空并在 open_questions 里说明，
不要给一个「大概是这个量级」的数字充当事实 —— 那比不给更糟，
因为它看起来像已知事实。
