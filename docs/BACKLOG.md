# 待办

按块组织，块内按依赖排序。每条都注明**为什么** —— 没有理由的待办过两周就没人知道该不该做。

标记：`[决定]` 待你拍板 · `[需要你]` 我做不了 · 其余我可以直接做。

---

## A. 自己定义专家的完整 workflow

目标：你只描述想要什么，系统帮你把它变成一个能跑、能验、能迭代的专家。
现在的状态是「只能改一个整体替换的 JSON 数组，改完只能跑整条编排来试」。

顺序有依赖，不能打乱：

1. ~~**任务信封结构化**~~ ✅ 已完成 —— `delegate({agent, goal, context, acceptance, why})`
   替掉现在的自由字符串 `task`，并定义专家实际收到的**渲染**。
   为什么先做：专家**看不到会话历史**（刻意设计，它没有对外身份），
   所以信封必须自足；而现在「包含必要上下文与验收标准」只是工具描述里的一句
   劝导，没有任何东西保证它真的包含。改成 schema 字段就从劝导变成契约，
   缺失率也能统计。
   附带：`why`（为什么选这个专家）让选择依据显式化，派错时它就是诊断材料。
   **必须先于 4/5**：`agent new` 的骨架里要写明「你的专家会收到什么」。

   已做：`src/runtime/envelope.ts`（校验 + 渲染 + 尺寸度量）、
   新规则 `delegate.envelope-complete`、`tool.intent` 里记下
   `target` / `why` / 各段长度（供第 15 条「委派准确率」用）、
   兼容老的 `{task}` 形状（数据库里已有的 run 不能因为 schema 变了就读不出来）。

   **遗留**：`test/fixtures/gemma4/orchestrator-delegate.json` 是真实录制，
   录制时参数还是 `{agent, task}`。已按新信封适配，原文保留在
   `adaptedFrom.originalArguments` 里可审计 —— 但那不再是纯粹的录制。
   `[需要你]` 在能访问 ollama 的机器上用新 schema 重新采集一次。

2. ~~**可声明的结果段**~~ ✅ 已完成 —— 替掉写死在 TS 里的 `'research' | 'code'`。
   现在 `CAPABILITY_SCHEMAS` 是硬编码的两个段，所以你没法让一个金融分析专家
   必须交出 `metrics[{name, value, asOf, source}]`。
   而 `resultJsonSchema` / `validateResult` / `agent show` 三处已经是数据驱动的，
   只是数据源写死了。
   **必须先于 5**：否则 LLM 生成定义时，输出形状只能在 research/code 里选。

   已做：小词表（scalar / `string[]` / `number[]` / `object[]`）、
   内置预设**用同一套词表表达**所以只有一条代码路径、声明本身在配置加载期
   校验（保留名、未知类型、object[] 缺 fields）、`resultJsonSchema` 里
   per-capability 的 if/else 全部删掉。

   一个立刻还债的例子：我一开始把 object[] 的元素限制成标量，结果**连
   research 预设自己都表达不了**（`findings[].sources` 是字符串数组）——
   用同一套词表表达内置预设的好处就在这里，词表不够用会当场暴露。

3. ~~**`agents/*.md` 加载器**~~ ✅ 已完成
   - 一个专家一个文件，天然增量（加文件不会删掉别人）
   - prompt 是正文而非 `\n` 转义串 —— 而 prompt 是你改得最勤的东西，
     diff 可读性直接决定迭代速度
   - **同时保留 `nucleus.config.json` 里的 `agents`**，doctor 报出每个 agent
     来自哪。留 `agentsDir` 口子给机器专属的专家
   - `agents/` **进 git**（定义要版本化、要能 diff、换机器要跟着走）；
     `nucleus.config.json` 保持 gitignore（机器相关：有哪些模型、数据库地址）
   - **试题集单独文件** `agents/<id>.cases.md`：定义保持短小可读，
     加一道题不显示成「这个 agent 变了」，而且「cases 不进 context」
     这件事从文件布局上就看得出来
   - 强断言：装配出的 prefix 里**不得出现试题文本**（与现有「前缀里不能有
     时间戳」同一类的守护）

   已做：极简 frontmatter 解析（标量 / 行内数组 / 嵌套映射，不引 YAML 依赖
   —— 完整 YAML 有锚点、多文档、隐式类型转换这些会咬人的特性，而我们只需要
   几个键值）、未知键报错而不是静默忽略、正文即 identity、`model` 支持单数
   写法、`agentsDir` 与 `NUCLEUS_AGENTS_DIR` 口子、`agent list` / `show`
   显示来源与试题数、三条「试题不进 context」的断言（包括 `cases` 写在定义
   文件里会被报成未知键 —— 结构上就进不去）。

   **文件优先于 JSON**：同 id 时以文件为准。「我改了 agents/x.md 却没生效」
   比反过来更难排查，而冲突会在 `agent list` 的来源列里显示成文件路径，
   所以不是无声的。

4. ~~**`nucleus agent new <id>`**~~ ✅ 已完成 —— 空骨架，离线可用。
   骨架里直接写 `whenToUse` 的正例反例：

   ```
   ✗ 调研                          ← 名词，模型难判断
   ✗ 负责研究相关任务              ← 同义反复
   ✓ 需要查外部资料、且结论必须带可验证来源时
   ```

   已做：骨架含 whenToUse 正例反例、权限连风险一起列、说明专家实际会收到
   什么（任务信封的形状）、列出现有专家提醒不要语义重叠、同时生成
   `.cases.md`、生成的骨架能直接加载不必先改。

   过程中踩了一个和「试题不进 context」同一类的坑：第一版把说明写在 markdown
   **正文**里、用 `#` 开头以为是注释 —— 但正文没有注释，`#` 是标题，而正文
   整段就是 identity。生成出来的骨架 prompt 有 **1504 字符 58 行**，全是给人
   看的说明。移进 frontmatter 的 `#` 行后降到 313 字符，并加了断言：
   prompt 里不得出现任何说明标记，且长度必须 < 500。

5. ~~**`nucleus agent new --describe "..."`**~~ ✅ 已完成 —— LLM 生成完整定义。
   之所以比一般的「让 LLM 写 prompt」靠谱：系统手里有它需要的全部素材
   —— 权限词表（含每项的 risk）、已注册工具（描述 + 所需权限 + 副作用等级）、
   现有专家名册（用来**避开语义重叠**）、结果契约选项。

   三条硬性设计：
   - **权限单独确认**。LLM 顺手给个 `execute` 完全可能，所以生成的权限要
     连风险一起列出来让你确认，不能默默生效
   - **必须真实模型**。没配模型时直接报错，而不是拿 mock 生成一份像样的垃圾
   - **说清它写不出什么**。LLM 不知道你的数据源和验收标准，生成的 `whenToUse`
     会「读起来合理但偏泛」。修正靠第 6 条那个循环，不是靠再生成一遍

   已做：提示词把真实目录全摊给模型（权限连风险、工具连所需权限、现有专家连
   职责）、产出用 function calling 而不是 markdown（好校验）、生成后拿真注册表
   校验（权限合法可授予 / 工具真实存在 / 结果字段声明过校验 / requiredFields
   引用的字段存在 / 与现有专家的职责重叠）、权限连风险列出并**必须显式确认**
   （非 TTY 下要 `--yes`，授权决定不该被管道悄悄通过）、只有 mock 时直接拒绝
   生成、模型还会提议 3-5 道试题作为回归集的种子。

   `A` 块**全部完成**。示例专家在 `examples/agents/`（不会被自动加载 ——
   产品不预设你需要什么专家），四个各压不同的地方：analyst 压自定义结果段、
   reviewer 压最小权限 + research 预设、librarian 压 fs 规则可达性、
   scout 压「声明了权限但没有对应工具」。

6. ~~**`nucleus agent try <id> "任务"`**~~ ✅ 已完成 —— 只跑这一个专家。
   现在试一个专家只能跑整条编排，几分钟一轮，而且结果不好时分不清是
   编排者派错了、任务描述太烂、还是专家本身不行。

   **从第一版就要带 `--n` 和 `--compare`**，不能先做单跑版 ——
   单跑版会让人以为「跑过了就是好了」，那比没有更糟。

   打印：调了哪些工具、**有没有想调没权限的工具**、契约过没过/退回几次/缺哪个
   字段、产出多大、token 与耗时。

7. ~~**回归判据**~~ ✅ 已完成（并进了 `agent try --compare`，不单独做 diff 命令）
   目标不是「证明更好」（单跑一次证明不了任何事，模型有随机性），
   而是**让退步无法悄悄溜过去**：

   - 固定试题集 —— **在不同任务上比两个定义，「更好」没有意义**
   - `--n 5` 重复跑取通过率，一次是噪音
   - `--compare HEAD~1` 与上一版并排（`agents/*.md` 在 git 里，上一版拿得到）
   - **权限面绝不能悄悄变宽** ← 纯静态、零成本、零噪音，能挡一大类真实退步
   - 越权尝试次数：它想调没权限的工具，说明 prompt 与权限不匹配

   能自动判的到此为止。**答案内容好不好系统判不了** ——
   要么你自己读（`nucleus artifact cat`），要么用裁判 agent 按信封里的
   `acceptance` 打分（这是把信封结构化的额外回报）。

   诚实的上限：在 3 道题上更好的定义，第 4 道题上可能更差。消不掉，
   只能让试题集随踩坑不断长大，逐渐变成回归集。

   已做：`--n` 重复取通过率、`--compare <git-ref>` 从 git 取旧版定义跑同一批
   题并排、权限面变宽独立标红（与试题表现无关）、越权尝试计数、
   输出里明写「答案内容好不好判不了」。试跑**不建会话**，与「子 run 无对外
   身份」保持一致；只含一个 agent 所以 delegate 不注册。

   顺带修了 mock 的一个坑：`whichAgent` 在脚本里找不到某个 agent 时会退回
   第一个，于是试一个**新**专家时它会拿到编排者的 delegate 剧本 ——
   而那个工具根本没注册。改成直接从 system prompt 抽 id，抽不到才兜底。
   现在 `agent try --mock` 对任何 agent 都能验「定义能不能加载、权限对不对、
   契约跑不跑」，而输出会明说这**不是**对专家的评估（没有模型参与）。

8. ~~**移除内置的 researcher / operator**~~ ✅ 已完成（并顺带收拢成一种来源）
   留 `orchestrator`（它是基础设施：入口 + 委派 + 整合，不是专家）。
   测试与 `verify` 改用导出的 `EXAMPLE_AGENTS` / `withExampleAgents()`。

   **顺带推翻了我自己的一个决定。** 第 3 条原本写「两种来源并存，不逼你选」，
   但那站不住：两条代码路径、两个出问题时要看的地方，而 `agent list` 的
   「来源」列本身就是我制造的歧义的补丁。和这个项目一贯的做法也矛盾 ——
   能力边界靠 permissions 而非工具名单、试题不进 prompt 靠结构而非承诺，
   都是「少一种机制、结构上不含歧义」。

   现在：`agents/*.md` 是**唯一**的编写方式；JSON 里写 `agents` 会被**拒绝**
   并给出迁移提示（静默忽略会让人以为配置生效了）；内置只剩 orchestrator
   作为兜底，同名 md 文件优先。

   还消掉了两个东西：`agents` 整体替换那个 footgun 不存在了；「有 delegate
   权限但没有可委派目标」不再是校验错误 —— 那是全新安装的正常状态，
   编排者直接作答是对的，而 delegate 工具在无目标时**不注册**
   （`enum: []` 不是有意义的 schema，给模型一个必然失败的工具等于宣告
   不存在的能力 —— 和当初那个假 web_search 同一个错）。

---

## B. 可靠性

9. ~~**run 级重试**~~ ✅ 已完成 —— 曾是剩下最大的可靠性洞，也是第 6 处「声明了没接线」。

   `recovery: 'automatic'` 界面说「系统会自动重试」，但 attempt 失败后 run
   直接落 terminal `failed`，队列是空的。真正的重试只发生在一次 `chat()`
   内部（就地重试 + 模型链降级）。`runStatusOverride`（注释写着「failed 但
   还要重试 → waiting_retry」）**没有任何调用方传它**。

   后果就是最初抱怨的那类：四个模型同时被限流 → run 直接死 → 得重新发一遍，
   而界面还说它会自己恢复。

   已做：退避 + 上限 + 调度（`run_queue.available_at` 现成，没建新表）、
   与终态写入**同事务**（否则中间崩溃会留下「waiting_retry 但队列为空」）、
   reconciler 兜底重排、事件流记 `attempt.failed{willRetry, delayMs, reason}`。

   **过程中被迫想清楚三件事：**

   一、`retryable` 与 run 级重试**不是一回事**。前者是「就地重试」（让模型
   重写），`contract.postcondition_failed` 标着 retryable: true。照它重试会把
   「模型答不对」变成「任务反复重跑，只是慢四倍」。所以新增 `runRetryable`，
   只给「等一会儿真的会好」的那几个：限流、额度、服务端错误、超时、全链不可用。

   二、**provider 说的时间不该被我们的退避上限截断。** `min(cap, max(backoff,
   after))` 是错的：额度一小时后重置而 cap 是 5 分钟时，我们会在 5 分钟后去撞
   一扇关着的门，把 attempt 预算烧完 ——「白等 15 分钟然后失败」比「等一小时
   然后成功」差得多。上限只约束我们自己猜的退避。

   三、决策必须放在 **runner** 的失败路径，不是 worker 的 catch —— 失败被
   runner 捕获并**返回** outcome，异常根本传不到 worker。我第一版放错了，
   结果 all_exhausted 照旧落 failed，实测才发现。

   顺带：`enqueueAttempt` 原本会把 `waiting_retry` 翻成 `pending`，那样「任务
   在等什么」就看不出来了 —— 改成只有立即可执行时才翻。以及界面的恢复性提示
   现在**跟着实际行为**而不只看错误码：run 是 waiting_retry 时显示
   「已排重试 · 2:24:36 PM 自动再试」，而不是 all_exhausted 的「需要你处理」。
   后者正是我之前批评过的「界面说谎」，差点自己又犯一次。

10. ~~**compact**~~ ✅ 已完成 —— 原来超预算是**丢**最旧的消息，不是压缩。
    装配器降级顺序里的 `shrink_summary` / `drop_summary` 两档永远不会触发，
    因为没有任何代码产生摘要。现在接上了。

    **摘要是结构化的，不是散文。** 这是这一块最要紧的决定。
    「请总结上面的对话」会产出「用户与助手讨论了若干话题」这种东西 ——
    读起来像摘要，但恰好丢掉了唯一要紧的部分。真实故障形状是
    「模型忘了我三轮前说过不要用 default 模型，又开始建议我加」，
    而散文摘要挡不住它，因为它没有任何地方**必须**写下约束。

    所以用小词表 + function calling（和结果契约同一套思路）：
    `constraints`（用户原话，渲染时排最前）/ `decisions`（连同为什么）/
    `open` / `artifacts`（只引用）/ `context`。模型只负责浓缩，
    什么必须留下由 schema 强制。

    三条不变量：

    - **消息永不删除。** 压缩只写 `summary` 与 `summary_through_seq`。
      摘要失败时退化成「装配器照常裁剪」，而不是数据丢失。
      库会一直长，但那是磁盘问题；反过来是正确性问题。
    - **压缩失败不能让任务失败。** 它是锦上添花的一次调用，所以整条路径
      catch 到底，只记账不抛。（隔壁 cron 是反例：一条坏计划不 catch 会拖死 worker。）
    - **压缩发生在装配之前。** 等装配器报 `trim_history` 才动手没用 ——
      那时消息这一轮已经被丢了。

    `compactions` 表一次一行（和 `schedule_fires` 同一个理由：有损、不可逆、
    没人在旁边看的操作必须自己留账）。存「哪一代退役了哪些 seq」与
    **摘要全文** —— 「模型为什么忘了」只能靠对比这两者定位，而症状要几轮之后
    才以「它怎么又提这个」的形式出现。压缩失败也记一行：那时消息没退役、
    摘要没生成，症状却和「摘丢了」一模一样，不留账就分不开。

    过程中修掉的：

    - **mock 会消耗 agent 的脚本游标。** 压缩请求没有 system 消息，
      `whichAgent` 认不出来会随便挑一个 agent，把后面的剧本全错位 ——
      症状是「某个专家突然走了别人的分支」，极难追。现在压缩请求单独认出来。
    - **mock 把我自己 prompt 里的「## 要求」当成用户约束。** 那会让测试因为
      **错误的原因**通过：断言「约束活下来了」，活下来的其实是提示词自己的话。
    - **`compactions` 按 `created_at` 排序不稳定**（FakeClock 下多行时间戳相同），
      于是「哪一代」会指错。改成按 `id`（bigserial）—— 与之前 `bundle.ts` 按
      `intent_at` 排序那个 bug 同一类。
    - 顺带：**`--conv` 只认完整 UUID，而终端打印的是 8 位短 id** ——
      界面给了一个粘不回去的值。现在按前缀解析，撞车时报出来而不是随便挑一个
      （挑错会把消息追加到别人的会话里）。

11. **装配器另外两个空入口** —— `facts`（**事实**层记忆）与 `constraints`（**提醒**注入）
    同样没人喂。`constraints` 归 C 块；`facts` 是「事实」层记忆，可以更晚。

---

## C. 规则

### 已完成：12（规则作者单元）+ 13（T1 注入）

`rules/*.md` —— 一个专家一个文件那套照搬过来，理由相同：T1 正文是改得最勤的
东西，写在 JSON 里是转义串、diff 读不出改了什么。

```
rules/cite-sources.md
---
appliesTo: ['*']
requiredFields: [findings[].sources]     ← T2
---
每条 finding 至少给一个可验证的来源。      ← T1
```

三层在 `agentSpec()` 里落地：T3 → `toolsDeny`、T2 → `requiredFields`、
**T1 → `spec.constraints`（不进 systemPrompt）**。后者是刻意的：缓存前缀要
逐字节稳定才能命中 prompt cache，而规则是会改的。

**两条原则由校验强制，不靠自觉：**

- **只有 T1 的规则被拒绝**（不是警告）。那等于「prompt 里写满禁止但模型照犯」，
  而且它会出现在规则清单里、看起来系统在管 —— **看起来有约束比没有约束更糟**。
  报错给的是**该问的两个问题**（能不能用 denyTools？能不能机械校验？），
  并允许「先不加」这个结论。
- `appliesTo` / `denyTools` 引用不存在的 agent / 工具**阻断加载**。
  拼错不会报错、只会静默失效 —— 这条校验立刻抓到了**我自己写的示例规则**里
  一个不存在的工具（`exec_shell`）。

T1 的**永久成本**在 `nucleus rules` 里显示出来：几条带正文、约多少 token、
以及「每一轮都占约束块预算，超出会被砍半」。加规则时看不到这个成本，
就会越加越多直到约束块被 `shrink_constraints`。

端到端测试读的是 **transcript**（模型实际收到的消息），不是「我们以为发了什么」
—— 因为「声明了没接线」的症状恰好是测试全绿。

### 已完成：大文本规则（索引 + 按需加载）

**问题是实测出来的。** 一份真实的规则集：18 个文件、正文共约 12k token
（我最初按字节估的 28k 偏高），单个最大 7k、最小 320。而末尾约束块的预算
只有 ~2000 token（131k 窗口）。

全塞进去的后果**不是报错，是被静默砍半**（`shrink_constraints`）——
而砍半的文档比没有更糟：前半段读起来像完整的规则，后半段（往往正是例外
与反例）不见了，且没有任何提示。

参考的是 OpenClaw 那套 `AGENTS.md` 索引：

```
- **工作区路径 → rules/workspace-paths.md** ⭐ 创建/部署前必读
- **环境参数 → rules/environment.md** ⭐ 禁止推测硬件
```

**关键不在「索引」本身，而在索引行里带着触发条件。** 「工作区路径规则」这种
索引是没用的 —— 模型不知道什么时候该去读。那份 AGENTS.md 自己限死 50 行，
把 12k 规则压成 1.2k 常驻。

已做：

- `gist` 字段 —— 永远在 context 里的那一行
- **自动分档**：正文 ≤ 120 token 直接内联；超过则只放 gist，正文按需
- **长正文缺 gist → 阻断**。不是「警告然后照样塞进去」
- `read_rule` 工具 —— 无需权限（索引说了「详见 read_rule(x)」却拿不到，
  那条规则就等于不存在；指向空处的索引比没有索引更糟）。没有按需规则时
  **不注册**它，与 delegate 同一个理由
- **gist 质量筛查**：不含任何触发词就提醒。机器判不了一句话是否真的可行动，
  但纯名词短语这种最典型的坏形状能抓 —— 中文祈使句几乎必然带
  「要 / 必须 / 前 / 禁止」之一

拿你真实的 18 条规则跑：**11,949 tok 正文 → 326 tok 索引，36.7 倍。**
而那次输出恰好证明了 gist 质量为什么要管 —— 我自动生成的
「communication —— 相关操作前必读」是废的，而「禁止推测硬件参数，必须先验证」
立刻可行动。

### 三层的名字（不用编号）

规则：**边界 boundary / 检查 check / 提醒 reminder**
记忆：**对话 history / 事实 facts / 检索 retrieval**

编号记不住哪个是哪个，而且**暗示顺序而不是强度** —— 「T1」听起来像
「第一层、最基本的」，而它恰恰最弱。那个误导正好助长了要修的毛病：
人的默认冲动是写一句 prompt 文本，编号让那感觉像「正常的第一步」。
「提醒」这个词本身就在提示它的弱。

`tiersOf()` 的返回顺序也改成强→弱；`nucleus rules` 里颜色即代价，
并列出三层各自的代价 —— 光有名字不够，代价也要说出来。

### 顺带修：门禁问错了问题

`oauth-auth-code.test.ts` 那 8 个测试需要**两种**能力：`listen` 一个端口，
以及 `fetch` 自己那个端口。而门禁只检查了前者 —— 开发机沙箱里这两件事是分开
授权的（绑定有时能成，Node 出网连 localhost 都 EPERM）。

后果真实发生过：`canListen` 通过 → 8 个跑起来 → 做 fetch 的那些失败，
而症状看起来像代码有问题。现在探测把两件事都做一遍，并打印跳过的**具体原因**。

门禁修好之后又见过一次失败（8 轮复现不了）。剩下的唯一脆弱点是**写死的端口
39001-39007** —— 偶发撞车，而这组平时跳过，所以只会在「偶然跑起来的那一次」
显形，看起来像随机失败。已改成临时端口（`freePort()`）。
部署机上仍是 48 passed / 0 skipped。


12. **规则作者单元** —— 一条规则同时携带三层，运行时按字段决定它落到哪层：

    ```jsonc
    { "id": "cite-sources",
      "constraint": "每条 finding 至少一个可验证来源。",   // T1 注入末尾约束块
      "check": { "requiredFields": ["findings[].sources"] }, // T2 校验
      "denyTools": [],                                       // T3 边界
      "appliesTo": ["researcher"] }
    ```

    两条原则：
    - **T1 必须配 T2**。只写 T1 等于把「prompt 写满禁止但模型照犯」原样搬回来
    - **能用 T3 表达的绝不写成 T1**。T3 零成本且不可违反；T1 只在无法用能力
      边界表达时才用（「结论要标来源」没有任何工具开关能阻止）

13. **T1 注入**到末尾约束块。`buildTail()` 与 `maxConstraintTokens` 预算都已存在，
    且刻意放在最后以免破坏前缀缓存 —— 只是没人调。

14. **per-rule 遵守率**。现在 `nucleus rules` 只能按**字段**统计。
    规则有了 id 之后就能按规则给出数字 —— 这才让「加规则」变成可收敛的动作。

15. **委派准确率**。数据是现成的（`delegate` 意图记了目标 agent，子 run 终态
    已知），所以「派给 X 的任务里多少失败/被退回」可以算。
    这比反复猜 prompt 措辞强，因为它可收敛。

16. ~~**`nucleus rule new`**~~ ✅ 已完成 —— 创建规则的 workflow

    **主路径是「说一句话，模型判层」，问答树是退路。**

    第一版只有问答树（先问边界、再问检查、最后提醒）。逻辑没错但**不直觉** ——
    实测反馈就是这个词。它把我的分类过程强加给使用者，而使用者心里想的是一句
    具体的要求，不是「这属于哪一层」。而「属于哪一层」恰好是模型擅长判的：
    它只需要理解那句要求的含义。

        nucleus rule new data-validation "金融数据必须标明来源、抓取时间与验证状态"

    模型提议完整规则（含分层与理由）→ 运行时校验 → 只有校验不过或它自己
    拿不准时才问你。`--interactive` 回到问答树；只有 mock 模型时自动退回
    （问一个 mock 等于拿一个假答案）。

    提示词里两件容易漏但关键的：

    - **给三层各自的代价**，不只是名字。否则模型会像人一样默认写提醒 ——
      那是最省事的一层。
    - **明确允许回答「无法可靠强制」**（`cannotEnforce`）。不然为了满足
      「提醒必须配检查」，它会编一个不相干的检查来凑数 —— 那比只有提醒更糟，
      因为看起来还多了一层保障。

    机器判不了的那一条会显式说出来：**「这个检查真的对应你那句要求吗？」**
    形式合法但不相干的检查，机器看不出来。

    有一条测试钉住「渲染出的 md 能被加载器原样读回来」—— 我在字段名上
    已经踩过一次（向导示例写 camelCase，加载器要 snake_case），不会再来一遍。

    那两条原则本身就需要一个**分类过程**才落得下去。而「我想加一条规则」时
    人的默认冲动恰好是最差的那个：直接写一句 prompt 文本。

    所以向导的核心价值不是帮你把 T1 写漂亮，是**按 T3 → T2 → T1 的顺序逼问**：

        ① 能用「不给工具」表达吗？    T3 —— 零成本、不可违反
        ② 能机械校验吗？              T2 —— 被拒时原文回给模型让它重做
        ③ T1 只是提示，不是执行手段    并且每轮都占约束块 token，永久成本

    **只有 T1 的规则要被拒绝**（或至少大声警告）—— 那等于把「prompt 写满禁止
    但模型照犯」原样搬回来，正是这个项目要修的第一个问题。

    与 `agent new --describe` 同一套分工：LLM 负责把一句话展开成措辞，
    **判定全部由运行时做** —— `appliesTo` 里的 agent 必须存在、
    `check` 引用的字段必须合法、`denyTools` 里的工具必须真注册过。

    还要显示**代价**：一条 T1 规则每一轮都吃约束块的预算，而且是永久的。
    加规则时看不到这个成本，就会越加越多直到约束块被砍半（`shrink_constraints`）。

---

### 17. check 的第四种：验**运行时事实**，而不是「模型提交了什么」

**发现方式：实测一条很典型的规则被判「强制不了」，而模型的理由是对的。**

    nucleus rule new plan-first "每次执行任务前必须写计划，计划写完必须由用户审核后同意后再执行"

gemma4 判 `cannotEnforce`，自己推出来的理由，一字不改地对：

> 这是跨回合的状态流转（计划 → 用户审核 → 执行），而非单次输出的静态格式。
> 虽然可以增加一个 `plan_approved` 字段并要求必填，但模型可以通过伪造该字段
> 直接跳过审核阶段。runtime check 只能验证结果中是否包含某个值，
> 无法验证用户在历史对话中是否真的表达了"同意"。

核对了：`validateResult(raw, spec)` 的 `raw` **就是模型自己写的那份结果**。
所以——

**今天所有的 check 都只校验模型自己的产出，因此都是可伪造的。**

平时不成问题，因为有效的 check 要求的是**内容本身**（数据带来源、结论带引用），
伪造那些等于把活干了。但主语一旦变成「模型自己声称做过某事」
（`plan_approved` / `verified` / `reviewed_by_user`），这一层就等于零 ——
而这类 check **最容易写出来**，加一个布尔字段就有了「检查」，
也最容易让人以为已经管住了。

所以缺的是第四种 check：**判据取自运行时记录，模型碰不到。**

- 调过 / 没调过某个工具（`tool_invocations` 已有全部数据）
- 有过一次用户审批（依赖 D）
- 先经过了某个 agent（`delegate` intent 已记录目标，依赖 H）
- 某个 artifact 存在且被某个 run 读过

数据基本都在，缺的是**表达它的地方**和执行点。`plan-first` 要的是前两条的组合，
所以它排在 D 之后。

**但真正更严重的问题在下面** —— 上面这些只是「说错了话」，这个是「算错了账」。

#### `cannotEnforce` 是整条规则的布尔值，而要求是复合的

plan-first 是**两句话**：

| 分句 | 今天能不能管 |
|---|---|
| 「每次执行任务前必须写计划」 | **能** —— 要求结果里有 `plan` 字段（带步骤）。伪造它等于真把计划写了，正是「伪造等于把活干了」那一类 |
| 「计划写完必须由用户审核同意后再执行」 | 不能 —— 要等审批原语 |

所以该给的是**拆分**，不是全盘否决。而模型给不出拆分，因为提示词里
**一个字都没提过复合要求**（核对过：`拆` / `分成` / `逐条` 全部零匹配），
只给了「全能 / 全不能」两个答案。更糟的是我加的 `cannotEnforce` 出口
让「全不能」成了最省事的那个答案。

真实的要求几乎都是复合的 —— 用户自己的规则就是多句的文档。
**不拆的话，每一条复合要求都会被向下取整到零。**

改动：

1. 提示词的**第 0 步**是拆句，并给出 plan-first 这个具体例子。
2. `uncoveredClauses` —— 没管住的分句，原样抄回来。
3. **`cannotEnforce` 与「已管住一部分」互斥，且是阻断的。** 两者同时为真时，
   向导会走 cannotEnforce 那条路（打印「强制不了」然后什么都不写），
   于是**已经找出来的那半个 check 被静默丢掉**。而模型混用它们很自然
   （「大体上强制不了，不过顺手加了个字段」）。
4. 树上也加了同一个出口，且不再提前放弃：记下管不住的那句，
   **接着问剩下的部分**。否则模型路径能拆、人的路径只能全要或全不要。

#### 关键的一半：没管住的分句必须**活到文件里**

打印一次是不够的。下个月 `nucleus rules` 上写着「plan-first：检查」，
没人会记得那条要求的后半句从来没生效过 —— 而 `tiersOf` 只看规则里**有什么**，
看不出要求里还有什么**没进来**。

这就是这个项目要修的第一个毛病降到了分句一级：
**看起来有约束比没有约束更糟。**

所以 `uncovered` 进 frontmatter、进 `UserRule`、进清单：

    ID          强制方式     作用于  必填字段（检查）
    cite        检查         全部    confidence
    plan-first  检查 半 (1)  全部    plan[].step

    ! 1 条只管住了一部分 —— 下面这些分句**没有任何机械强制**，靠模型自觉：
      plan-first
        · 计划写完必须由用户审核后同意后再执行

改这个顺带发现 frontmatter 解析器**不支持块状列表**（只有行内 `[a, b]`）。
不能用行内：行内按 `,` 切，而没管住的分句是散文，里面本来就可能有逗号 ——
那时一句话被切成两句，**而且不报错**，清单上只是多出一条半截的句子。
给解析器加了 `- ` 支持（agent 文件也一起受益）。

还顺手删了树自己那份 md 渲染器 —— 它和 `renderRuleMd` 并存，加 `uncovered`
时要改两处，漏一处的症状是「树生成的规则少了一半信息」且不报错。
`finish` 本来就已经拼出一个 `UserRule`，用同一个渲染器。

**另外三处修掉的**（都在 `rule new` 里，都是这次实测暴露的）：

1. **`cannotEnforce` 混了两件事。** 「本质判不了」（语气、行文）该写进 identity；
   「运行时缺原语」**绝不该**写进 identity —— 那是把一个能修的缺口埋成一句
   没人强制的话，从此没人会再想起它。加了 `unenforceableKind` 区分，
   建议完全分开，并列出已知缺口。
2. **退回问答树之后，树把人问到了 `requiredFields: [open_questions]`** ——
   一个和 plan-first 毫无关系的检查。树只有「能 / 不能」两个答案，
   「能判，但要判的不是我提交的东西」这个真实答案无处可去。
   加了第三个选项，直接停下并说明缺什么。
   **模型路径防住了这件事，人的路径没防住** —— 而这正是 `cannotEnforce`
   存在的目的。
3. **「这个检查真的对应你那句要求吗」原先只在 `--describe` 路径上问。**
   树上更需要问：一路按回车挑个字段就成了。

## D. 编排者作为对话层

16. **`ask_user`** —— 编排者现在**不能反问用户**。全项目没有这个工具，
    唯一的人工介入是 `needs_human_confirmation`（副作用未知时），
    那是个死胡同状态而不是对话。需求含糊时它只能猜。

    复用 wake 机制，几乎不用新东西：问题以 assistant 消息写进会话 → run 落
    `waiting_user`、attempt 结束（不占进程不占 context）→ 用户下一条消息
    就是唤醒信号。与 `delegate` 完全同构。

    **已定：锁。** 你下一条消息就是对那个提问的回答；想开新任务用 `/new`。
    不锁的话系统得猜「这句是答案还是新任务」，而猜错的代价是把回答喂给
    错误的 run。

    实现上不用新东西：`conversations.acquire()` 已经用条件更新做好了
    「同会话只允许一个活跃 run」的 CAS。**但它目前只有自己的测试在调用**
    （第 9 处「声明了没接线」）—— 所以这一条其实是两件事：接上那把锁，
    再加 `waiting_user` 状态。

    今天还没出问题只是因为 CLI 与 REPL 都是串行的；一旦有并发入口
    （或者两个 CLI 同时跑同一个会话）就会两个 run 抢一个会话。

17. **名册进 segment ②**。现在只在 `delegate` 的工具描述里（选择发生的地方，
    这是对的）。system prompt 里再放一份**决策方法**（怎么拆解、什么该自己答），
    但**不放工具/权限清单** —— 编排者不调那些工具，给它看会诱导越级指挥，
    还把它和会变的工具名耦合起来。

---

## E. cron ✅ 已完成

18. ~~**`schedules` 表 + worker tick 里的 `#fireDueSchedules()`**~~ ✅ 已完成

    不需要新进程：`run_queue.available_at` 已经有了，定时的本质就是
    「到点往队列塞一个 run」，和 reconciler 同一个位置（`worker.tick()` 里，
    在 `claimNext` **之前** —— 这样刚到点的任务同一轮就能被领走）。

    **每次新建会话，不注入上次结果。** 每次运行都是一件独立的新工作，
    只告诉它任务目标。附带好处：会话不会无限增长，**cron 因此不依赖 compact**。

    以后若想要「别重复昨天已经报过的内容」这类跨运行的记忆，那属于「事实」层
    事实层，不是会话连续性 —— 两者不要混。

    落地时改了两处设计，都是被测试逼出来的：

    - **`next_fire_at` 的推进基准是 `now`，不是最后一个计划点。**
      从计划点往后推的话，一个「逾期三小时」的每小时任务会在 `next` 仍然
      `<= now` 时被下一个 tick 再次领走，一路补到追平 —— 那恰恰是
      `catch_up=false` 要避免的「笔记本合盖一晚上，醒来跑 8 次」。
    - **补偿要反向取。** `plannedBetween` 的上限卡在前端，拿到的是**最早**的
      N 个；补跑日报你要昨天那份，不是三天前那份。所以加了 `prevFireAt` /
      `latestPlanned`，迭代次数恰好等于 N，与关机时长无关。

    三个语义的最终选择：
    - **停机补偿**：per-schedule 的 `catch_up`，默认**关**；`catch_up_max`
      默认 3。开了也只补最近 N 次。
    - **重入**：上次没跑完就**跳过**，不排队 —— 每小时一次的任务如果单次要跑
      两小时，排队会越积越多，而「跳过」在语义上是对的（同一件工作还在做）。
    - **幂等键**：`sched:<id>:<计划时刻截到分钟>`，靠 `runs.idempotency_key`
      的 unique 约束挡重复。**用唯一索引而不是先查后写** —— 两个 worker 同时
      到点是 TOCTOU，查重挡不住。

    **`schedule_fires` 表（一个计划点一行，append-only）**是这块最重要的一个
    补充，它不在原计划里。理由：`run_events.run_attempt_id` 是 not null，
    而**被跳过的触发根本没有 run**。而「今天早上 8 点那次到底跑没跑」恰恰是最
    需要事后能查的事 —— 没有这张表，症状就只是「产出不再出现」。

    `planned_at` 与 `fired_at` 分两列存：差值就是延迟/补偿的证据。

    连带修掉的一类**显示性谎言**：`outcome='fired'` 只说明**触发**成功，
    run 可能随后就失败了。对着一个 `config.agent_not_found` 的 failed run 打
    绿勾，和之前「系统会自动重试」那句假话是同一类错误。所以 `history`、
    `doctor`、诊断包三处都按 **run 的最终状态**判定，不按触发结果。

    入口：`nucleus schedule list|add|rm|enable|disable|history`。
    `doctor` 会报出指向不存在 agent 的计划（这是「产出静默消失」最常见的成因），
    诊断包把计划定义与最近 10 次触发结果一并带上。

---

## G. 可追溯性 ✅ 已完成

24. ~~**transcript：模型被问了什么、答了什么**~~

    事件流对「发生了什么」是完整的，但对「模型为什么那么做」是瞎的：
    `llm.call.started` 只有 `{step, chain}`、`llm.call.finished` 只有 token 与
    延迟、工具实参只有 `args_hash`（`args_ref` / `result_ref` 是第 10、11 处
    「声明了没人写」）、bundle 里 agent 只有 id 列表、artifact 没有内容。

    于是远程诊断答不了这个阶段最常见的问题：编排者为什么派给了这个专家？
    专家为什么忽略了验收标准？

    「事后重建」不成立：重建需要当时的 config + agent 定义 + 当时的历史，
    三样都会变；而且出问题之后再想开启记录就来不及了。所以**默认开**。

    已做：`transcripts` 表（完整消息数组 + 回复 + tool_calls 实参，超长截断
    并标记）、`tool_invocations.args_json` / `result_text`、bundle 带上
    agent 完整定义 + 来源 + 工具目录 + artifact 内容（单条 64KB 上限）+
    transcripts、`nucleus replay` 渲染成因果链。

    可以用 `runtime.captureTranscripts: false` 关掉。

25. ~~**dataDir 解析统一**~~ —— 同一个 bug 犯了三次（某个命令自己写 dataDir
    时漏掉 `NUCLEUS_PGLITE_DIR`，于是悄悄打开另一个空库，表现成「明明跑过
    任务却说没数据」）。抽成 `resolveDb(flags)`，消掉这一类而不是这一处。

---

26. ~~**provider 层的可追溯性**~~ ✅ 已完成

    `provider_state` 只有**当前快照**，所以这些答不出：熔断什么时候打开的、
    因为什么、开到几点？那次 429 试了链上哪几个模型、各自返回什么？
    **为什么用了链上第 3 个**（前两个为什么被跳过）？

    新增 `provider_events`（append-only）：`picked`（含**被跳过的候选与原因**
    —— 以前只在全链失败时才有）、`ok`（延迟/用量/学到的额度）、`failed`
    （错误码 + 根因提示 + 连续失败数）、`breaker.open/half_open/closed`
    （从什么状态到什么状态、开到几点）、`exhausted`（**「429 打挂整条链」
    就是这一条** —— 每个模型各自为什么不可用、最早几点恢复）。

    并把 `usage_log` 接上（第 12 处「声明了没人写」）—— 逐次调用的
    用量与成本，挂在 attempt 上，所以「这次 429 是哪个任务触发的」答得出。

    `nucleus providers [log]` 把它们变成能回答问题的视图：当前状态、
    失败聚合（带恢复性）、跳过原因、全链不可用、用量成本、逐条时间线。

    **仍然缺的是「妥善解决」那一半**：全链不可用时 run 直接失败，
    没有 run 级重试（B9）。日志现在能告诉你「等到 18:38:58 就恢复了」，
    但系统不会自己等。

---

## F. 诚实性与技术债

19. ~~**README / DESIGN 的现状段改准**~~ ✅ README 已改（DESIGN 待查）。
    原来说「前沿模型遵守率未知」（真实 GLM 已验，0 次契约退回）、
    把「context 装配」列在已完成里（接线之前那句不成立）、
    还把 `toolsAllow` 当主闸门（现在 permission 才是）。
    **一份会骗人的现状清单比没有更糟。**

19b. **`oauth-auth-code.test.ts` 里有 8 个测试在本机永远跳过**
    （`describe.skipIf(!canListen)`，本机 `listen` 是 EPERM）。

    这不是无害的：有一次全套跑出 `1 failed | 742 passed` 且**没有 skipped**
    —— 沙箱那一瞬间允许了绑定端口，于是这 8 个跑了起来，其中一个失败。
    之后 16 轮（含 3 轮满负载）都复现不了，因为 `listen` 又变回 EPERM。

    读代码定位到的成因是 `close 之后端口被释放` 那条：`close()` 是
    fire-and-forget，测试 sleep 50ms 就去重新绑同一个端口，负载下不够。
    已修 —— `close()` 改成返回 Promise。这不只是测试的方便：**回调端口是固定的**
    （provider 注册的 redirect_uri 写死了端口），所以「登录失败后重试」必然要
    重新绑同一个端口，而不能 await 就只能 sleep 一个猜出来的时长。

    ✅ **已在部署机上验证**：`48 passed`，**0 skipped**，含那 8 个。
    修复有效，这一条关闭。

19c. **需要在部署机上做的验证清单**（我这个沙箱执行不了的部分）

    | 要验什么 | 命令 | 为什么我做不了 |
    |---|---|---|
    | ~~OAuth 回调服务器 8 个测试~~ | ✅ 已验：48 passed / 0 skipped | 本机 `listen` 是 EPERM |
    | ~~tier 3 真模型~~ | ✅ 已跑通：7 passed（但暴露了下面 19d） | Node 出网是 EPERM |
    | **真模型会不会委派** | `npm run test:live` 里新加的那条 | 需要 gemma4:31b |
    | **compact 对真实模型的效果** | `nucleus conv compact <id>` 然后自己读一遍 | 只有 mock 摘要器跑过，而那是我写的 |
    | TUI 交互 | `nucleus chat` | 没有 TTY |
    | cron 跨真实时间 / 夏令时 | 设一条 `*/5 * * * *` 放一小时 | 只用 FakeClock 与一次 62 秒实测验过 |

    **compact 那条是最值钱的。** 这个功能的全部价值是「用户提过的约束能活过
    压缩」，而 mock 摘要器是我写的，它当然会把约束抄下来。真实模型会不会把
    约束放进 `constraints`、会不会在第二代压缩时丢掉，目前**一点数据都没有**。

19p. **`--overflow` 溢出探测删掉了** ✅

    我给的理由是「请求在生成之前就被拒，所以几乎不花钱」——**那是假设，
    不是事实**：有的厂对被拒的请求照样按输入计费，更糟的是有的厂**接受并截断**，
    那就真的处理了 3M token 的输入。把一个没验证的成本假设做成功能是不行的。

    解析器（`parseWindowFromError`）留着：**真实调用**撞到窗口上限时报错里就带
    这个数字，那时不额外花钱。将来可以在 `provider.bad_request` 的处理里顺手记。

19q. **provider 与 model 混在一起** ✅ 已修

    `ModelConfig` 里塞了两类东西：

        provider 的：baseUrl / api / apiKeyRef / rpm / tpm / anthropicVersion
        model 的：  model / contextWindow / maxTokens / 单价

    而**同一个模型跑在不同 provider 上是常态** —— anthropic 的 opus-5、
    openrouter 的 moonshotai/kimi-k3、ollama 的 kimi-k3。三条都要重抄一遍
    baseUrl / api / apiKeyRef，而**抄漏一处不会报错**，只会在调用时 401；
    那时人会去查凭据，不会想到是配置抄漏了。

    **顺带一个真 bug**：`rpm`/`tpm` 是账号级限制，却放在 model 上、令牌桶按
    模型 key 记 —— 同 provider 两个模型各拿一个桶，配 rpm=60 实际发到 120，
    然后一起撞 429。桶改成按 provider 记之后自然消失。

    已做：`providers` 配置段 + `resolveModels()`（分层是配置要的，扁平是运行时
    要的，这个函数就是那道边界）。规则是「**provider 提供默认，模型写了就覆盖**」
    而不是二选一 —— 同 provider 下某个模型走不同端点是真实存在的
    （Kimi 的 coding 端点）。旧写法（无 providers 段）照旧有效。

    `PROVIDER_TEMPLATES` 内置 7 个 provider 的**端点与协议**，
    但**没有凭据、也不猜 contextWindow** —— 有测试钉住这两条。

19s. **`nucleus model config` 交互式向导** ✅ 已做

        ① 选 provider
        ② 选凭据方式（API key / OAuth）并确认凭据在位
        ③ 列出这个 provider 下**可用的模型**，选一个
        ④ 确认窗口与输出上限

    **②③ 的顺序是依赖决定的，不是偏好。** 云端的「列出可用模型」走
    `GET /v1/models`，那需要凭据 —— 反过来的话第二步必然失败，而那不是 bug。
    ollama 是例外（`/api/tags` 不需要凭据），所以它那条路径最短。

    每一步都可能「问不出来」，那时必须让人接手：列不出模型就手打 id，
    窗口探不到就让人填，**绝不给一个猜的默认值**。

    OAuth 没配 clientId 时显示成**不可选并说明为什么**，而不是藏起来 ——
    藏起来等于让人猜为什么少了一项。

    实现上撞了两个 stdin 的坑，都是「不报错的那种」：

    - 每次 `readLine` 新建再 `close` 一个 readline 接口。「用完就关」听起来
      很干净，但 `close()` 把 stdin 一起带走 —— 第二次读**什么都拿不到而且
      不报错**，症状是向导问完第一个问题就卡住。
    - 改成共享一个之后：管道输入时 readline 会把能读的**一次全读进缓冲**，
      然后在 EOF 关闭接口 —— 哪怕后面的问题还没问，直接抛
      `readline was closed`（一句和「输入」毫无关系的话）。

    最终：**管道下一次读完 stdin、按行出队**（既正确又可测，测试直接喂字符串
    数组），TTY 下才用 readline（要它的行编辑）。

19r. **`nucleus model add` 非交互形式** ✅ 已做

        nucleus model add anthropic  claude-opus-5
        nucleus model add openrouter moonshotai/kimi-k3
        nucleus model add ollama     kimi-k3

    两段：先确认 provider（内置模板一句话搞定，不在列表里就给 `--base-url`），
    再问模型自己的东西。窗口的处理顺序是**能问服务就问服务，问不出来就问你，
    绝不猜**：不给 `--context-window` 时先探测（ollama `/api/show` 权威），
    探不到就退出并说清「这一项没法猜」+ 填错的后果。

    `key` 是**别名**、`model` 是真实 id ——`openrouter:kimi-k3` 指向
    `moonshotai/kimi-k3`，链里引用短名。

    **不自动写回配置**：配置里那些注释（每个数字为什么是这个值）会被 JSON
    序列化全部丢掉。这和 `agent new` 不同 —— 后者是**新建**文件，没有既有注释可毁。

    加完会直接算给你看新模型的预算与压缩触发点。`model set` 改窗口时显示前后对比。

19o2. **`NODE_TLS_REJECT_UNAUTHORIZED=0` 现在会被 doctor 报出来** ✅

    做 model add 时从 Node 的警告里发现的（在开发机的 shell 里设着）。
    它让**所有** HTTPS 跳过证书校验，包括 Nucleus 带着 API key 去调 provider
    的那些 —— 中间人能拿到凭据，而且**没有任何迹象**：请求照样成功。

    常见成因是绕公司 MITM 代理。那个需求是真的，但正确做法是
    `NODE_EXTRA_CA_CERTS` 指向公司根证书 —— 只多信一个 CA，而不是谁都信。
    doctor 里那条检查就是这么写的。

19n. **窗口只能靠手填，而我自己就填错了一个** ✅ 已修

    上一条那张对比表里我写了「GLM-5.2 窗口 205k」—— 那个数字是从
    `nucleus.config.example.json` 抄的，而那个文件里的注释自己就写着
    「contextWindow 不知道就留空，宁可不填、不编造数字」。**我用一个可能过时的
    数字做了对比表**，而模型版本更新比任何一份代码的知识都快。

    根子上的问题是：这个数字**只能手填**，填错**不会报错** —— 填小了压缩过早
    触发（1M 窗口的模型在 3% 就开始压），填大了请求被拒或者被静默截断。

    已做 `nucleus providers probe`，三个来源可靠性递减：

    | 来源 | 可靠性 | 说明 |
    |---|---|---|
    | ollama `/api/show` | 权威 | `model_info["<arch>.context_length"]` |
    | provider `/v1/models` | 看运气 | OpenAI 规范里**没有**这个字段，多数厂不给 |
    | `--overflow` 溢出探测 | 最低 | 发超长请求，从 **provider 自己的报错**里读 |

    **每个结果都带来源。** 一个从错误文本正则出来的数字与一份官方元数据可信度
    不是一回事，不该显示成同一样。而且**不自动写回配置** —— 探测会出错，
    静默改配置会让一个错数字变成「已知事实」；只打印可以直接粘的片段。

    ollama 那条附带一个比数字本身更重要的前提：`/api/show` 给的是**训练窗口**，
    不等于 ollama 实际分配的（由 `num_ctx` 决定，老版本默认 4096）。
    那种情况下按训练窗口装配会被**静默截断**，两边对不上而且不报错。

    顺带把 example 里那个 `204800` 删了（改成留空 + 指向 probe），
    并给 ollama 那个示例数字加上「这是示例不是你的真实值」。

19o. **看不到当前会话的 context 占用** ✅ 已修

    加了 `ctx 84.6K/131K (65%)`，接在每轮的尾行：

        (=^ω^=)/ 1 个 run · 240 tok · $0 · 39ms · ctx 166/20.0K (1%)

    `tok` 与 `ctx` 是**两个含义完全不同的数字**：前者是这一轮花掉的 token
    （计费），后者是这一轮发出去的 context 有多大（离撞墙还有多远）。
    混成一个会让人以为「才花了 2k token，离窗口还远」，而 context 已经 80% 满。

    数字取自 `context.assembled` 事件的 `breakdown.total` —— 那是装配器写的、
    **真的发出去的那一份**，不是估算。自己估会与实际漂移，而这个数字的全部
    用处就是「离撞墙还有多远」，漂了就没意义。

    取**根 run 的最后一次**：编排者整合那一轮 context 最大，子 run 不含会话
    历史，拿它报会低估。70% 标黄（压缩触发线附近）、90% 标红（再长就装不下）。

19m. **预算写死 40k，与模型窗口无关；判据用条数而不是 token** ✅ 已修

    窗口本来就是按模型取的（`contextWindowFor` 取链上最小值），但预算被一个
    与窗口无关的硬上限卡住：

        historyBudgetFor(window) = min(40_000, window - 16_000)

    | 窗口 | 旧历史预算 | 旧触发 | 新历史预算 | 新触发 |
    |------|-----------|-------|-----------|-------|
    | 131k | 40.0k | 28.0k | **84.6k** | 59.2k |
    | 205k | 40.0k | 28.0k | **129.0k** | 90.3k |
    | 1M   | 40.0k | 28.0k | **705.3k** | 493.7k |

    **1M 窗口的模型会在用掉 3% 的时候开始压缩** —— 而每次压缩是一次模型调用
    加一次不可逆的信息损失，窗口里还空着 97%。

    `reserveForOutput: 16_000` 写死同样错：对 maxTokens=8192 的模型多留一倍，
    而在 20k 窗口的模型上它一个人吃掉 80% 的可用空间。正确来源是**模型自己
    声明的输出上限** —— 那才是「它可能吐多少」。

    改法（`src/context/budget.ts`）：

        输出余量 = 链上最大 maxTokens × 1.25   （下限 1k，最多占窗口一半）
        历史预算 = (窗口 − 输出余量) × 0.7
        摘要 / 约束块 = 按比例，但有上下限

    摘要有上限是刻意的：1M 窗口下让摘要涨到 80k 没有意义 ——
    摘要的价值在于短，长到那个程度不如直接放原文。

    输出上限取链上**最大**值而窗口取**最小**值 —— 两个方向都保守。降级到一个
    配了更大 maxTokens 的模型时，按小的那个留余量会让它一吐长就撞窗口。

    **判据也从条数改成 token**：
    - `keepRecent: 10`（条）→ `keepRecentRatio: 0.3`（占预算的比例）+
      `keepRecentMin: 2`（下限）。一条粘贴的日志可能顶几十条对话，
      「最近 10 条」在那种情况下能占满整个窗口。
    - `minRetire: 3`（条）→ `minRetireTokens: 2000`。退役 3 条小消息省不下什么，
      退役 1 条巨大的日志能省很多 —— 值不值得取决于 token。
    - 判定顺序也改了：**token 阈值是第一判据**，然后才算退役哪些。

    预算也进了 `context.assembled` 事件 —— 「为什么这一轮裁得这么狠」取决于
    当时算出来的预算，而不是某个常量。

19l. **两条触发条件里有一条从来不改变结果** ✅ 已修

    判定原本是三条：`fresh < minMessages(8)` → token 阈值 → `retireCount <= 0`。
    而第一条被第三条**严格包含**（`fresh < 8` 一定也 `fresh <= 10`），
    所以它从来不改变结果，只换一句措辞 —— 而那句措辞还不如第三条准确：
    5 条时真实原因是「不够 10 条，没有可退役的」，不是「不值得调一次模型」。

    有一条测试就叫「消息太少就不压缩 —— 不值得一次模型调用」并断言
    `reason` 含「不值得」。**测试名与断言一起指向错误的原因，而它照样是绿的。**

    `minMessages` 的**本意**是对的（别为一点点东西调一次模型），但它盯错了
    对象：该盯**要退役多少条**，不是总共有多少条。真正没被挡住的是另一头 ——

        fresh = 11, keepRecent = 10  →  退役 1 条

    这种情况原来**会真的压缩**：调一次模型给一条消息做摘要，纯浪费。

    改成 `minRetire`（默认 3，作用在 retireCount 上）。手动压缩降到 1 ——
    `conv compact` 的语义是「我现在就要压」，「值不值得」该由人判断。

19k. **降级顺序把摘要整个丢掉，包括用户约束** ✅ 已修

    读代码解释规则时发现的，不是测出来的。装配器超预算时原本是：

        裁历史 → drop_summary（整个丢） → 砍约束块 → 失败

    第二步把摘要**整个丢掉，包括里面全部用户约束**。而 compact 存在的唯一理由
    就是保住那些约束 —— 到了最缺预算的时候反而第一个丢它，自相矛盾。

    而且**摘要是结构化的，本来就能按段降级** —— 这正是结构化的好处，
    却完全没被用上。丢掉 `context` 那段散文能省下大部分体积，
    而 constraints 全部保住。

    已加一档 `summary_to_constraints`，排在 `drop_summary` 之前：
    只留「要求 + 未决」。装配器仍然只按预算取舍、不懂摘要的内部结构 ——
    最小形态由调用方渲染（`renderSummaryMinimal`）后一起传进去。

    连约束都没有时 `renderSummaryMinimal` 返回空串，让上层直接走 drop ——
    一个只有标题的摘要是纯浪费。

    终端上两档分开显示：「摘要降到只剩要求」与「丢弃摘要（连用户约束一起）」
    —— 严重性差别很大，混成一句话会让人以为都一样。

19j. **四个场景的真实数据：模型三个全对，我的检查器错了一个。**

    gemma4:31b，`basic` / `decoys` / `revision` / `implicit` 各跑一次。

    - **`decoys` 真的全对。** 三条诱饵（「心跳不能经过模型对吧？」这类用了
      「必须/不能」的技术陈述）一条都没进 `constraints`，全被归到「背景」。
      **它分清了「这是你的要求」和「这是系统的事实」** —— 而这两者在措辞上
      几乎没区别。我原本预计会漏一两条。
    - **`revision` 模型对、我的检查器误报。** 摘要写的是「部署机上就用真
      Postgres，本地才用 PGlite」，旧结论一点痕迹都没留 —— 完全正确。
      但检查器把所有 constraints **拼成一整块**再查 `['postgres','pglite']`，
      两个词当然都命中，命中的是**新状态那一行**。
      这正是我自己写在文档里的局限（查词不查意思），咬在了最要紧的那条检查上。
      已改成**逐行匹配**，并给会被撤销的约束加 `staleKeywords`（旧结论的标志词，
      如「不要 + postgres 同行」），与话题词分开。用那次的真实输出重放，三条全过。
    - **`implicit` 两条委婉表达都被识别成要求。** 也超出预期。

    **一个我没设计到的问题**：三次运行里 `悬而未决` 每次都有一条，而且每次都是
    一条**已经被回答过的填充问题**。它在为了填满字段而找一条 —— 这类噪音会逐代
    累积。已在字段描述里明说「不要为了填满去翻一条已回答的提问，空着完全正常」。

    结论：**gemma4:31b 的摘要质量比我预期好**，三类难形状都过了。
    还缺多代累积的数据。

19i. **「3 条约束都在 ✓」是过度解读，seed 已重写。**

    第一版只埋三句固定的话，固定在第 2/5/9 轮。而那三句都是**最好认的形状**：
    以明确标记开头（「有一条你要一直记着」）、都用「不要 / 必须」、都是自足单句。

    **对着 n=3 的固定集调 prompt 就是过拟合。** 而且它测不到三种真会出问题的：

    - **诱饵** —— 用了「必须 / 不能」但是技术陈述，不是用户要求。混进
      `constraints` 会让约束段逐代变脏，而原来那版「3/3 ✓」照样报绿
    - **撤销** —— 约束后来被取消。摘要留着已撤销的约束**比丢掉一条更糟**：
      模型会照它拒绝你现在想要的东西，而你不知道它为什么在拒绝。
      这一类我第一版完全没想到
    - **委婉表达** —— 没有「不要 / 必须」，关键词根本筛不出来

    重写成四个场景（`basic` / `decoys` / `revision` / `implicit`），判定从
    「活下来了吗」变成四种：`ok` / `lost` / `leaked` / `stale`。
    `--scenario` 选场景，埋点信息进消息 meta，事后可完整还原。

    实测有效：mock 摘要器（纯关键词匹配）在 `decoys` 场景下把三条诱饵全放进了
    `constraints`，被检查器逮住 —— 那正是旧版测不到的形状。

    仍然是**筛查不是判定**：查词不查意思，`implicit` 那类本来就筛不出来。
    输出里明说了这一点。

19h. **compact 对真实模型的第一份数据** ✅ 已取到

    gemma4:31b，15 轮合成历史（3 条埋好的约束 + 12 条普通技术提问），
    压缩 2.4k → 348 tok（省 85%）。

    做对的几件事，其中两件我没料到：

    - 三条约束全部保留，而且**把口语框架剥掉了**：「有一条你要一直记着：
      不要有任何 default 模型…」→「不要有任何 default 模型…」。
      比逐字照抄好 —— 留了实质，丢了框架。
    - **零误报**：12 条普通提问一条都没混进 `constraints`，而是被归到
      `context` 里一句话概括。这一项比「约束有没有丢」更能说明它是否理解了
      字段语义，也是我更担心的一项。
    - `decisions` 是真从助手回复里提炼的（判定与执行分离 / 终态不可回改 /
      序号用数据库单调值），不是复述问题，而且两条带了「为什么」。
    - 「相关答案对应在 DESIGN.md 的第 1 至 13 节」是**对的** —— 退役了 13 轮，
      每轮回复里写的是第 i+1 节。它做了正确的区间聚合，不是编的。

    **一处真问题**：`artifacts` 里出现了 `DESIGN.md` 与 `agents/*.md` ——
    那不是这个系统登记过的产出，只是对话里提到的文件名。

    这是**我的字段描述含糊，不是模型的错**：原文写的是「提到过的产出路径或
    ref」，它照「提到过的路径」理解了。后果会累积：下一轮可能去读一个不存在的
    artifact，而且这条会被每一代继承。

    已做两件事，缺一不可：
    - 描述改清楚（「本系统登记过的产出」，并点名 README.md / DESIGN.md 不算）
    - **机器核对**：`reconcileArtifacts()` 拿 artifacts 表对一遍，对不上的
      拿掉并在 `compact.finished` 事件里报出 `artifactsDropped`。
      光靠 prompt 不够 —— 产出登记在表里，是可核对的事实。

    结论：**compact 从「机制跑通、质量零数据」变成「有一份真实样本，
    质量可接受」。** 但那份样本只覆盖了最容易的形状 —— 见 19i。
    还缺：诱饵 / 撤销 / 委婉三个场景的数据，以及多代累积后有没有漂。

19g. **我自己的编辑方式漏掉了修改，而 tsc 照样过。**

    `test/live/ollama.test.ts` 是我唯一跑不了的文件（Node 出网 EPERM）。
    我用字符串替换改它，**不匹配时静默什么都不做** —— 而 `tsc` 通过，
    因为旧代码本身是合法 TypeScript。于是「已修」的两处一路带到部署机，
    第二轮跑出**一模一样的错误**。

    根因不是手滑，是**判据住在跑不到的代码里**。所以把它挪出去了：
    `src/runtime/stuck.ts` + `test/stuck.test.ts`（10 条，本地可跑）。
    挪的过程中立刻发现那段 SQL 还有第二个错列名（`run_queue.run_attempt_id`，
    那张表的键是 `(run_id, attempt_no)`）—— 也就是说就算第一处修对了，
    第二轮还是会炸。

    顺带纠正一句我说错的话：`tool_invocations` **是**存实参的
    （`args_json`，migration 0003 加的），我说「刻意不落库」是错的。
    列名写错和「不存在」是两件事。

    产出不只是测试：「有 run 挂住了吗」本来就该是产品能回答的一句话，
    现在 `nucleus doctor` 会报，连同 `non_idempotent` 的未知结果
    （那是「绝不能自动重跑」的那一类）。

19f. **请求超时硬编码 120 秒，而且没法配置** ✅ 已修

    `RouterOptions.timeoutMs` 一直存在，但 **boot 从来不传** —— 于是唯一生效
    的值是 `openai-compat.ts` 里硬编码的 120 秒。第 7 处「声明了但没接线」。

    实测暴露的：gemma4:31b 写一份调研报告超过 120 秒 → `provider.timeout`。
    而 timeout 是 `runRetryable`，所以任务进 `waiting_retry` 再跑一遍 ——
    **花两倍时间，然后同样超时**。整条 live 测试跑了 280 秒就是这么来的。

    太短的代价比太长严重得多：太长只是「多等一会儿才降级」。

    已做：`runtime.requestTimeoutMs`（默认 300 秒，按本地模型取值）+
    **每个模型自己的 `timeoutMs`**。差异住在模型上 —— 本地 31B 要十分钟，
    云端 2 分钟很宽裕，一个全局值必然对一边太紧。超时报错现在说清该调哪个旋钮。

    有测试钉住「配置真的传到了 provider」—— 这是这一类 bug 唯一挡得住的方式。

19d. **一条 live 测试绿着，而它声称验的事根本没发生。**

    实测输出：

        > 委派 → 专家干活 → wake 唤醒 → 整合 → 回写会话
        [live] run 树：orchestrator(failed)      ← 却是 ✓

    两处都错：
    - 名字承诺了委派，但**没有任何一条断言碰到委派**
    - root 状态的断言是 `['succeeded','failed'].includes(status)` ——
      对两种结果都通过，所以永远抓不到失败

    关键区别是「小模型能力不够」和「系统坏了」，而这条断言把两者混成一件事：
    「所有请求都连不上 provider」这种回归也会显示绿色。

    已改：名字只承诺断言覆盖到的东西（「跑完后没有任何悬挂状态」）；
    failed 只接受**能力类**错误码，provider / config / runtime 类一律算破了。

    **改完第一次跑就抓到了东西**，而且抓到的是我自己的三个错：

    - `CAPABILITY_FAILURES` 是**凭记忆写的**，`budget.max_steps` /
      `budget.max_tokens` / `rule.violation` 三个码根本不存在。实测报的
      `budget.no_progress` 才是真的。已按 `errors.ts` 重写。
    - 我又写了条守卫去防这个，而**守卫本身犯了同一类错**：
      `errorSpec()` 未知码返回 `null` 不是 `undefined`，所以
      `=== undefined` 那条检查永远不会失败。现在有一条测试钉住守卫真的有效。
    - 新加的委派测试查 `tool_invocations.args` —— **那个列不存在**。
      表里只有 `args_hash` / `args_ref`，完整参数刻意不落库。信封应该从
      **子 run 的 `input`** 读，那反而更好：那是真正到达专家手里的那份。

    另外「不能有 waiting 状态的 wake」这条断言是错的：它把「正在等一次已排好
    的重试」误判成故障。实测撞上了（`orchestrator(waiting_children) →
    researcher(waiting_retry)`）。改成**非终态必须有东西会推动它** ——
    队列里有记录（available_at 可以在未来）或在等还活着的子 run。
    这才是「任务挂住却看不出来」那条不变量的正确形状。

    **好消息：gemma4:31b 真的会委派。** 这条此前完全没有数据。

19e. **`conv compact` 的两个诊断谎言**（都由实测输出暴露）

    ① `--dry-run` 用的策略和真正执行时不一样，所以**它预测不了真跑的结果**。
    同一个 4 条消息的会话：dry-run 说「不值得调一次模型」（配置里的
    minMessages: 8），真跑说「单条消息过大」（手动策略 minMessages: 2）。
    两个互相矛盾的原因，而且都没说对。已抽出 `manualPolicy()` 共用。

    ② 「单条消息过大，压缩无从下手」这句话**从来没描述过它自己的条件**。
    它的分支条件是 `retireCount <= 0`，等价于 `fresh.length <= keepRecent`
    —— 纯粹是条数，与消息大小毫无关系。所以它在一个总共 40 tok 的会话上
    也会被打出来。已删掉，改成如实说「只有 N 条，少于要保留的 K 条」。

    真正的「压缩也救不了」是另一种形状：退役完之后**留下的那几条本身**就
    超预算。那时压缩会成功但装配器仍要裁剪 —— 现在会在理由里说出来。

20. **6 张建了从没写过的表** —— 每张代表一个只有 schema 的功能：
    `tasks` + `activity_logs`（任务看板 / 计划式编排）、`prompt_versions`
    （prompt 版本化与归因）、`tool_snapshots`（工具集快照）、`mcp_servers`
    （MCP 运行时状态持久化，现在只在内存里、重启就丢）、`usage_log`
    （逐次调用用量明细，现在只有 attempt 级聚合）。

    要么接上，要么删掉。留着是误导 —— 看表判断进度会系统性高估。

21. **自动检查「声明了没接线」**。这一路撞了 8 次：
    `ModelConfig.contextWindow` / `run_attempts.context_breakdown` /
    `run_attempts.provider+model` / 整个 `context/assemble.ts` /
    `messages.tokens` / `runStatusOverride` / `artifacts.content+sha256` /
    写死的能力段 / `conversations.acquire+release`（会话串行锁，只有自己的
    测试在调）。加上 6 张空表、装配器 3 个空入口，以及同样没人调的
    `conversations.fork`（编辑重发）。

    规律很清楚：**这个仓库里 schema 和类型跑得比实现快。** 至少该有一条
    「每张表都有 INSERT」「每个声明的字段都有写入方」的检查跑在 CI 里。

22. `[需要你]` **跑一次 `npm run test:live`**。tier 3 我写了但一次没执行过
    —— 沙箱禁止 Node 出网，连 localhost 都是 EPERM。只验了「环境不可达时的
    跳过路径」与 typecheck。

23. `[需要你]` **在真终端里开一次 `nucleus chat`**。带框输入区的实际手感我
    验不了（起不了 pty），只用假 tty 测了转义序列。不对劲用
    `NUCLEUS_NO_BOX=1` 退回行提示符。

---

## H. 多 agent 来回交流（orchestrator ⇄ A ⇄ B）

22. **现在是单向流水线，想要的是多轮协商。**

    现在：`orchestrator → 专家A → 结果 → orchestrator → 专家B → 结果`。
    一次传输、拿结果、结束。想要的是三方来回：A 做到一半问 B 一句、B 回，
    A 再问编排者确认一个前提，然后才交付。

    直觉上产出会更好，理由也站得住：一次性传输要求上游**在开始前就把信封
    写全**，而很多前提是做到中途才发现缺的。现在缺前提的专家只有两条路 ——
    自己猜，或者在 open_questions 里留一句然后交差。两条都不好。

    ── **但这条要动的是最核心的不变量，不能顺手加** ──────────

    a) **wake/join 现在是一次性的。** 父 arm 一个 wake 等 N 个子，子写终态
       → 递减 → 归零唤醒父。而「问一句等一句」要求子 run **不进终态就能等**
       —— 那是一个新状态（`waiting_reply`），形状与 `ask_user`（D 块）完全
       一样：一个 run 等一个外部答复。**所以 D 是这块的前置**，不是并行项。

    b) **A ⇄ B 是兄弟通信，而现在只有父→子。** `delegate` 的语义是「派生一个
       子 run」，不是「给某人发消息」。兄弟通信需要：寻址（A 怎么指名 B？
       用 agent id 还是 run id？），以及一个新权限 —— 现在的 `delegate`
       授权的是「可以派活」，不等于「可以找任何人搭话」。

    c) **终止性是最大的风险。** 两个 agent 可以无限来回，而且每一轮都在花钱。
       `maxRunsPerRoot: 32` 管不住这个 —— 来回不新建 run，只是多几个 attempt。
       需要一个**按消息轮次**的预算，且超了要有明确的终止语义
       （谁做最终决定？编排者，还是各自交付现状？）。

    d) **context 会炸，而且炸在一个现在还没有 compact 的地方。**
       A 要不要看到完整的 A⇄B 线程？要看，context 就随轮次线性增长。
       而**子 run 现在没有 conversation、没有历史**（这是刻意的不变量：
       「子 run 没有对外身份」），也就是说压缩、预算、装配这一整套目前
       只服务 root run。给子 run 加线程等于给它们加历史。

    e) **「子 run 没有对外身份」这条不变量要不要破？** 不要。来回交流需要的是
       一个 run 之间的**线程**对象，与用户可见的 `conversations` 是两回事。
       混用会让专家的过程直接漏给用户 —— 那正是这个项目一开始要修的问题。
       所以需要新对象（暂称 `threads`），而不是复用 conversations。

    f) **「效果更好」目前是推测，得能量出来。** 同一个任务、同一批试题，
       单向 vs 多轮，比通过率与步数 —— `agent try --compare` 已经是这个形状，
       扩一下就能用。**先能量再优化**，否则会为了一个没验证的直觉付出
       3-5 倍的调用成本。

    建议顺序：**D（ask_user / waiting_reply）→ 兄弟寻址与权限 → 轮次预算与
    终止 → threads 对象与它的 context 装配 → 拿 try --compare 量效果。**
    在 D 之前动这块，等于同时开两个新状态机。

---

## 更远（不在当前计划里）

- **HTTP API + SSE** —— 你说过 TUI 够用、这个不重要。事件流的旁路
  （`TeeEventSink`）已经为它准备好了
- **事实层 / 检索层记忆** —— 检索层在 migration 头部就写明推后 v2
  （pgvector 本地不可用）
- **计划式编排**（plan → step → 依赖图）—— 现在是 ad-hoc 委派 + wake
- **approval 流程** —— 现在只有 `needs_human_confirmation` 一种人工介入

---

## 建议顺序

**A（1→2→3→4→6→7→5→8）** → **G（可追溯性）** → **B9（run 级重试）**
→ **E（cron）**
→ **B10（compact）** ← 到这里都已完成
→ 接下来 **C（规则）** → **D（ask_user）** → **H（多 agent 来回交流）**
→ **F（诚实性与技术债）**

**H 排在 D 之后不是偏好，是依赖**：来回交流需要「一个 run 不进终态也能等一个
答复」，那和 `ask_user` 是同一个状态机。在 D 之前动 H 等于同时开两个新状态机。

E 提前了：cron 定成「每次新建会话」之后不再依赖 compact，而它是独立的一块，
不阻塞别的。事后看这个顺序是对的 —— cron 反而暴露了「触发成功 ≠ 跑成功」
这类显示性问题，而那类问题在 compact/规则上会以同样的形状再出现。

A 里把 5（LLM 生成）放在 6/7 之后：先有能验的循环，再让 LLM 往里灌内容，
否则生成出来的东西无从判断好坏。

F19（改准现状段）随时可做，且最影响你判断「还差多少」。
F21（自动检查）建议在 A 完成后立刻做 —— A 会引入不少新的声明式字段。
